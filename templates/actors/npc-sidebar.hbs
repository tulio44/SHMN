<div class="sidebar">

    {{!-- HD --}}
    {{#if important}}
    {{#with system.attributes.hd}}
    <div class="pills-group empty">
        {{!-- TODO: Allow clicking this to roll a hit die --}}
        <h3 class="icon">
            <i class="fa-solid fa-dice-d{{ denomination }}"></i>
            <span class="roboto-upper">{{ localize "SHMN.HitDice" }}</span>
            <span class="counter">{{ value }} &sol; {{ max }}</span>
        </h3>
    </div>
    {{/with}}
    {{/if}}

    {{!-- Speed --}}
    {{> "shmn.actor-trait-pills" values=speed key="speed" icon="fas fa-shoe-prints"
        label="SHMN.Speed" configAction="movement" }}

    {{!-- Skills --}}
    <div class="pills-group {{#unless skills}}empty{{/unless}}">
        <h3 class="icon">
            <i class="fas fa-briefcase" inert></i>
            <span class="roboto-upper">{{ localize "SHMN.Skills" }}</span>
            {{#if editable}}
            <button type="button" class="config-button unbutton" data-action="showConfiguration" data-config="skills"
                    data-tooltip="SHMN.SkillsConfig" aria-label="{{ localize 'SHMN.SkillsConfig' }}">
                <i class="fas fa-cog" inert></i>
            </button>
            {{/if}}
        </h3>
        <ul class="pills">
            {{#each skills}}
            <li class="pill">
                <a class="{{ @root.rollableClass }} skill-name" data-key="{{ key }}"
                   data-action="roll" data-type="skill">
                    <span class="label">{{ label }}</span>
                    <span class="separator">&vert;</span>
                    <span class="value">{{ shmn-numberFormat total signDisplay="always" }}</span>
                </a>
            </li>
            {{/each}}
        </ul>
    </div>

    {{!-- Senses --}}
    {{> "shmn.actor-trait-pills" values=senses key="senses" icon="fas fa-eye"
        label="SHMN.Senses" configAction="senses" }}

    {{!-- Resistances --}}
    {{> "shmn.actor-trait-pills" values=traits.dr key="dr" color="green" icon="fas fa-shield-halved"
        label="SHMN.Resistances" }}

    {{!-- Immunities --}}
    {{#if editable}}
        {{> "shmn.actor-trait-pills" values=traits.di key="di" color="green" icon="fas fa-shield"
            label="SHMN.DamImm" }}
    {{else}}
        {{> "shmn.actor-trait-pills" values=traits.di key="di" color="green" icon="fas fa-shield"
            label="SHMN.Immunities" }}
    {{/if}}
    {{> "shmn.actor-trait-pills" values=traits.ci key="ci" color="green" svg="rosa-shield"
        label="SHMN.ConImm" }}

    {{!-- Vulnerabilities --}}
    {{> "shmn.actor-trait-pills" values=traits.dv key="dv" color="maroon" icon="fas fa-heart-crack"
        label="SHMN.Vulnerabilities" }}

    {{!-- Damage Modification --}}
    {{> "shmn.actor-trait-pills" values=traits.dm key="dm" icon="fas fa-heart-circle-plus"
        label="SHMN.DamageModification.Label" }}

    {{!-- Languages --}}
    {{> "shmn.actor-trait-pills" values=traits.languages key="languages" icon="fas fa-flag"
        label="SHMN.Languages" }}

    {{!-- Habitat --}}
    {{> "shmn.actor-trait-pills" values=habitat configAction="habitat" icon="fa-solid fa-mountain-sun"
        label="SHMN.Habitat.Configuration.Label" }}

    {{!-- Treasure --}}
    {{!-- TODO: Allow clicking a treasure category pill to roll appropriately for random treasure --}}
    {{> "shmn.actor-trait-pills" values=treasure configAction="treasure" icon="fa-solid fa-gem"
        label="SHMN.Treasure.Configuration.Label" }}

</div>
