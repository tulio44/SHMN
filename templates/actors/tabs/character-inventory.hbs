<section class="tab {{ tab.cssClass }}" data-tab="{{ tab.id }}" data-group="{{ tab.group }}">

    {{!-- Encumbrance & Containers --}}
    <div class="top">

        {{!-- Encumbrance --}}
        <div class="encumbrance card">
            {{> "systems/shmn/templates/inventory/encumbrance.hbs" encumbrance=system.attributes.encumbrance
                hasBreakpoints=true }}
            <div class="info">
                <div class="strength">
                    <span class="label">{{ localize "SHMN.AbilityStr" }}</span>
                    <span class="value">{{ system.abilities.str.value }}</span>
                </div>
                <div class="size">
                    <span class="label">{{ localize "SHMN.Size" }}</span>
                    <span class="value" aria-label="{{ size.label }}">{{ size.abbr }}</span>
                </div>
                <div class="multiplier">
                    <span class="label">{{ localize "SHMN.Multiplier" }}</span>
                    <span class="value">
                        <span class="sign">&times;</span>
                        {{ size.mod }}
                    </span>
                </div>
            </div>
        </div>

        {{!-- Containers --}}
        {{> "systems/shmn/templates/inventory/containers.hbs" }}

    </div>

    {{> "systems/shmn/templates/inventory/inventory.hbs" }}
</section>
