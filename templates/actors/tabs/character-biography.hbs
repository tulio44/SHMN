<section class="tab {{ tab.cssClass }}" data-tab="{{ tab.id }}" data-group="{{ tab.group }}">

    {{!-- Characteristics --}}
    <div class="top">
        <ul class="unlist characteristics">
            {{#each characteristics}}
            <li>
                <label>
                    <span class="roboto-upper label">{{ localize label }}</span>
                    {{#if @root.editable}}
                    <input type="text" name="{{ name }}" value="{{ source }}">
                    {{else}}
                    <span class="value">{{ value }}</span>
                    {{/if}}
                </label>
            </li>
            {{/each}}
        </ul>
    </div>

    {{!-- Ideals, Bonds, Flaws, Personality Traits, & Appearance --}}
    <div class="middle">
        <div class="left">
            {{~> "shmn.biography-textbox" name="system.details.ideal" value=system.details.ideal label="SHMN.Ideals"
                icon="fas fa-seedling" class="textbox-half" ~}}
            {{~> "shmn.biography-textbox" name="system.details.bond" value=system.details.bond label="SHMN.Bonds"
                icon="fas fa-link" class="textbox-half" ~}}
            {{~> "shmn.biography-textbox" name="system.details.flaw" value=system.details.flaw label="SHMN.Flaws"
                icon="fas fa-heart-crack" class="textbox-half" ~}}
        </div>
        <div class="right">
            {{~> "shmn.biography-textbox" name="system.details.trait" value=system.details.trait
                label="SHMN.PersonalityTraits" icon="fas fa-puzzle-piece" class="textbox-half" ~}}
            {{~> "shmn.biography-textbox" name="system.details.appearance" value=system.details.appearance
                label="SHMN.Appearance" icon="fas fa-image-portrait" class="textbox-half" ~}}
        </div>
    </div>

    {{!-- Notebook --}}
    <div class="bottom" data-target="system.details.biography.value">
        <h3 class="icon">
            <i class="fas fa-feather"></i>
            <shmn-icon src="systems/shmn/icons/svg/ink-pot.svg"></shmn-icon>
            <span class="roboto-upper">{{ localize "SHMN.Notebook" }}</span>
        </h3>
        {{#if editable}}
        <prose-mirror name="system.details.biography.value" value="{{ source.details.biography.value }}"
                      document-uuid="{{ actor.uuid }}"></prose-mirror>
        {{else}}
        <div class="editor">
            <div class="editor-content">{{{ enriched.value }}}</div>
        </div>
        {{/if}}
    </div>

</section>
