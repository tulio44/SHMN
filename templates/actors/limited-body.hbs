<section class="tab active" data-tab="biography" data-group="primary">

    {{!-- Biography --}}
    {{#if enriched.value}}
    <div class="bottom">
        <h3 class="icon">
            <i class="fas fa-feather" inert></i>
            <shmn-icon src="systems/shmn/icons/svg/ink-pot.svg"></shmn-icon>
            <span class="roboto-upper">{{ localize enriched.label }}</span>
        </h3>
        <div class="editor">
            <div class="editor-content">{{{ enriched.value }}}</div>
        </div>
    </div>
    {{/if}}

</section>
