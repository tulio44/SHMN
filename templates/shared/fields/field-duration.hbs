{{!-- Duration --}}
<div class="form-group split-group">
    <label>{{ localize "SHAMN.Duration" }}</label>
    <div class="form-fields">

        {{!-- Amount --}}
        {{#if duration.scalar}}
        {{ formField fields.value value=data.value label="SHAMN.Amount" localize=true hint=false placeholder="—"
                     classes="label-top" rootId=partId }}
        {{/if}}

        {{!-- Time --}}
        {{ formField fields.units value=data.units label="SHAMN.DurationTime" localize=true hint=false
                     options=durationUnits classes="label-top" rootId=partId }}
    </div>

    {{!-- Conditions --}}
    {{#if (eq source.duration.units "spec")}}
    {{ formInput fields.special value=data.special input=inputs.createTextInput classes="full-width"
                 placeholder=(localize "SHAMN.DURATION.FIELDS.duration.special.label") rootId=partId }}
    {{/if}}
</div>
