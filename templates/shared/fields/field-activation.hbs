{{!-- Activation --}}
<div class="form-group split-group">
    <label>{{ localize label }}</label>
    <div class="form-fields">

        {{!-- Amount --}}
        {{#if activation.scalar}}
        {{ formField fields.value value=data.value label="SHAMN.Amount" placeholder="—" localize=true hint=false
                     classes="label-top" rootId=partId }}
        {{/if}}

        {{!-- Type --}}
        {{ formField fields.type value=data.type label="SHAMN.Cost" localize=true classes="label-top" hint=false
                     options=activationTypes rootId=partId }}
    </div>

    {{!-- Condition --}}
    {{ formInput fields.condition value=data.condition input=inputs.createTextInput classes="full-width"
                 placeholder=(localize "SHAMN.ItemActivationCondition") rootId=partId }}
</div>
