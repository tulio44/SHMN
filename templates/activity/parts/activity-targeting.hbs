<section class="tab activity-{{ tab.id }} {{ tab.cssClass }}" data-tab="{{ tab.id }}" data-group="{{ tab.group }}">
    <fieldset {{ disabled disabled.range }}>
        <legend>
            <label class="checkbox">
                {{ localize "SHMN.RANGE.FIELDS.range.label" }}
                {{#if (or activity.range.canOverride activity.isRider)}}
                {{ formInput fields.range.fields.override value=source.range.override input=inputs.createCheckboxInput
                             dataset=(shmn-object tooltip="SHMN.RANGE.FIELDS.range.override.hint")
                             ariaLabel=(localize "SHMN.RANGE.FIELDS.range.override.hint")}}
                {{/if}}
            </label>
        </legend>
        {{> "shmn.field-range" range=activity.range fields=fields.range.fields data=data.range rangeTypes=rangeUnits
            inputs=inputs }}
    </fieldset>

    {{> "shmn.field-targets" target=activity.target fields=fields.target.fields data=data.target source=source.target
        inputs=inputs disabled=disabled.target }}
</section>
