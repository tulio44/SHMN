<section>
    {{~#if hasConcentration}}
    <fieldset>
        <legend>{{ localize "SHMN.Concentration" }}</legend>
        {{#each fields}}
            {{ formField field name=name value=value options=options input=input }}
        {{/each}}
    </fieldset>
    {{> "shmn.activity-usage-notes" }}
    {{/if~}}
</section>
