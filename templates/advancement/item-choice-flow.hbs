<form id="{{ appId }}" data-level="{{ level }}" data-id="{{ advancement.id }}" data-type="{{ type }}"
      data-current="{{ choices.current }}" data-max="{{ choices.max }}">
    <h3>{{{ title }}}</h3>

    <div class="drop-target">
        {{#if advancement.hint}}
        <p>{{ advancement.hint }}</p>
        {{/if}}

        {{#if replaceable}}
        <div class="item-name">
            <label class="flexrow">
                <h4>{{ localize "SHMN.ADVANCEMENT.ItemChoice.Replacement.None" }}</h4>
                <input type="radio" name="replace" value="" {{ checked noReplacement }}>
            </label>
        </div>
        {{/if}}

        {{#if abilities.options}}
        <div class="form-group">
            <label>{{ localize "SHMN.SpellAbility" }}</label>
            <select name="ability" {{ disabled abilities.disabled }}>
                {{ selectOptions abilities.options selected=abilities.selected }}
            </select>
        </div>
        {{/if}}

        {{#each previousLevels}}
        <h4 class="form-header">
            {{#if (ne @key "0")}}
            {{ localize "SHMN.AdvancementLevelHeader" level=@key }}
            {{else}}
            {{ localize "SHMN.AdvancementLevelAnyHeader" }}
            {{/if}}
        </h4>
        {{#each this}}
        <div class="item-name flexrow {{#if replaced}}replaced{{/if}}">
            <div class="item-image" style="background-image: url('{{ img }}');"></div>
            <label class="flexrow">
                <h4><a data-uuid="{{ uuid }}">{{ name }}</a></h4>
                {{#if (and @root.replaceable (not replaced))}}
                <input type="radio" name="replace" value="{{ id }}" {{ checked checked }}>
                {{/if}}
            </label>
        </div>
        {{/each}}
        {{/each}}

        <h4 class="form-header">
            {{ localize "SHMN.ADVANCEMENT.ItemChoice.Chosen" current=choices.current max=choices.max }}
        </h4>

        {{#each items}}
        <div class="item-name flexrow" data-uuid="{{ uuid }}">
            <div class="item-image" style="background-image: url({{ img }});"></div>
            <label class="flexrow">
                <h4>
                    <a data-uuid="{{ uuid }}">{{ name }}</a>
                </h4>
                {{#if dropped}}
                <button type="button" class="unbutton control-button item-control item-delete" data-action="delete"
                        data-tooltip aria-label="{{ localize 'SHMN.ItemDelete' }}">
                    <i class="fa-solid fa-trash" inert></i>
                </button>
                <input type="hidden" name="{{ uuid }}" value="true" data-dtype="Boolean">
                {{else}}
                <shmn-checkbox name="{{ uuid }}" {{ checked checked }} {{ disabled disabled }}></shmn-checkbox>
                {{/if}}
            </label>
        </div>
        {{/each}}

        {{#if advancement.configuration.allowDrops}}
        <div class="pill-lg roboto-upper empty" data-action="browse">
            {{ localize selectLabel }}
        </div>
        {{/if}}
    </div>
</form>
