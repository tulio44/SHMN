<fieldset class="card items table">
    <legend>{{ localize "SHMN.ADVANCEMENT.Migration.Selection" }}</legend>
    <div class="form-group hint">
        <p class="hint">{{ localize "SHMN.ADVANCEMENT.Migration.Hint" name=item.name }}</p>
    </div>
    <div class="header">
        <div class="item-name">{{ localize "SHMN.AdvancementTitle" }}</div>
        <div class="item-controls">{{ localize "SHMN.Add" }}</div>
    </div>
    <ol class="items-list unlist">
        {{#each advancements}}
        <li class="advancement-item item flexrow">
            <div class="item-name flexrow">
                {{#if svg}}
                <shmn-icon src="{{ icon }}"></shmn-icon>
                {{else}}
                <img class="gold-icon" src="{{ icon }}" alt="{{ title }}">
                {{/if}}
                <span class="name">{{ title }}</span>
            </div>
            <div class="item-controls flexrow">
                <shmn-checkbox name="{{ id }}" checked></shmn-checkbox>
            </div>
            {{#if summary}}
            <div class="item-summary">
                {{{ summary }}}
            </div>
            {{/if}}
        </li>
        {{/each}}
    </ol>
</fieldset>
