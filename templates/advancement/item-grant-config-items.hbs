<fieldset class="card items table">
    <legend>{{ localize "SHMN.ADVANCEMENT.ItemGrant.FIELDS.items.label" }}</legend>
    <div class="header">
        <div class="item-optional">{{ localize "SHMN.ADVANCEMENT.ItemGrant.FIELDS.optional.label" }}</div>
        <div class="item-name name-header">{{ localize "DOCUMENT.Item" }}</div>
    </div>
    <ol class="items-list unlist">
        {{#each items}}
        <li class="item flexrow" data-item-uuid="{{ data.uuid }}">
            <div class="item-optional">
                {{ formInput fields.optional name=(concat "configuration.items." @index ".optional") value=data.optional
                             input=@root.inputs.createCheckboxInput }}
            </div>
            <div class="item-name">
                {{{ shmn-linkForUuid data.uuid renderBroken=true }}}
                <input type="hidden" name="configuration.items.{{ @index }}.uuid" value="{{ data.uuid }}">
            </div>
            <div class="item-controls flexrow">
                {{#if @root.editable}}
                <a class="item-control item-action" data-action="deleteItem" data-tooltip
                   aria-label="{{ localize 'SHMN.ItemDelete' }}">
                    <i class="fas fa-trash" inert></i>
                </a>
                {{/if}}
            </div>
        </li>
        {{/each}}
    </ol>

    <div class="form-group">
        {{#if showContainerWarning}}
        <p class="hint centered warning">{{ localize "SHMN.ADVANCEMENT.ItemGrant.Warning.Container" }}</p>
        {{/if}}
        <p class="hint centered drop-area">{{ localize "SHMN.ADVANCEMENT.ItemGrant.DropHint" }}</p>
    </div>
</fieldset>
