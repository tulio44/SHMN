<div class="flexrow">
    {{~#if displaySelection}}
    <div class="count {{#if invalid}}invalid {{/if}}">
        {{{ localize "SHMN.CompendiumBrowser.Selection.Label" summary=summary }}}
    </div>
    <button type="submit"{{#if invalid}} disabled data-tooltip aria-label="{{invalidTooltip}}" {{/if}}>
        {{ localize "SHMN.CompendiumBrowser.Selection.Select" }}
    </button>
    {{/if~}}
</div>
