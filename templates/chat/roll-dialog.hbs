<form>
    {{#each formulas}}
    <div class="form-group">
        <label>{{ localize "SHMN.Formula" }} {{#if type}}({{type}}){{/if}}</label>
        <input type="text" name="formula" value="{{formula}}" disabled>
    </div>
    {{#if types}}
    <div class="form-group">
        <label>{{ localize "SHMN.DamageType" }}</label>
        <select name="roll.{{ @index }}.type">
            {{ selectOptions types }}
        </select>
    </div>
    {{/if}}
    {{/each}}
    {{#if attackModes}}
    <div class="form-group">
        <label>{{ localize "SHMN.ATTACK.Mode.Label" }}</label>
        <select name="attackMode">
            {{ selectOptions attackModes }}
        </select>
    </div>
    {{/if}}
    {{#if ammunitionOptions}}
    <div class="form-group">
        <label>{{ localize "SHMN.CONSUMABLE.Type.Ammunition.Label" }}</label>
        <select name="ammunition">
            {{ selectOptions ammunitionOptions blank="" }}
        </select>
    </div>
    {{/if}}
    {{#if chooseModifier}}
    <div class="form-group">
        <label>{{ localize "SHMN.AbilityModifier" }}</label>
        <select name="ability">
            {{selectOptions abilities selected=defaultAbility labelAttr="label"}}
        </select>
    </div>
    {{/if}}
    {{#if masteryOptions}}
    <div class="form-group">
        <label>{{ localize "SHMN.WEAPON.Mastery.Label" }}</label>
        <select name="mastery">
            {{ selectOptions masteryOptions }}
        </select>
    </div>
    {{/if}}
    <div class="form-group">
        <label>{{ localize "SHMN.RollSituationalBonus" }}</label>
        <input type="text" name="bonus" value="" placeholder="{{ localize 'SHMN.RollExample' }}"/>
    </div>
    <div class="form-group">
        <label>{{ localize "SHMN.RollMode" }}</label>
        <select name="rollMode">
            {{selectOptions rollModes selected=defaultRollMode localize=true}}
        </select>
    </div>
</form>
