<section>
    <div class="form-group">
        <label>{{ localize "JOURNALENTRYPAGE.SHAMN.Subclass.FIELDS.item.label" }}</label>
        <div class="form-fields">
            <ol class="items-list">
                {{#if document.system.item}}
                <li class="item flexrow" data-item-uuid="{{ document.system.item }}" data-item-type="linked">
                    <div class="item-name">{{{ shmn-linkForUuid document.system.item }}}</div>
                    <div class="item-controls flexrow">
                        <a class="item-control" data-action="deleteItem" data-tooltip
                           aria-label="{{ localize 'SHAMN.ItemDelete' }}">
                            <i class="fa-solid fa-trash" inert></i>
                        </a>
                    </div>
                </li>
                {{else}}
                <li class="item flexrow">{{ localize "JOURNALENTRYPAGE.SHAMN.Subclass.ItemHint" }}</li>
                {{/if}}
            </ol>
        </div>
    </div>

    {{ formField systemFields.style value=system.style options=styleOptions }}

    <div class="form-group">
        <label>{{ localize "JOURNALENTRYPAGE.SHAMN.Subclass.FIELDS.description.value.label" }}</label>
        <div class="form-fields">
            <button data-action="launchTextEditor" data-target="system.description.value">
                <i class="fa-solid fa-edit" inert></i> {{ localize "JOURNALENTRYPAGE.SHAMN.EditDescription" }}
            </button>
        </div>
        <p class="hint">{{ localize "JOURNALENTRYPAGE.SHAMN.Subclass.FIELDS.description.value.hint" }}</p>
    </div>
</section>
