<section>
    <div class="distribution">
        <span>{{ localize "SHMN.Award.Distribution.Split" }}</span>
        <slide-toggle name="each" {{ checked each }}></slide-toggle>
        <span>{{ localize "SHMN.Award.Distribution.Each" }}</span>
    </div>
    {{#unless hideXP}}
    <label class="xp currency-style">
        <span class="roboto-upper">{{ localize "SHMN.ExperiencePoints.Label" }}</span>
        {{ numberInput xp name="xp" class="uninput" min=0 step=1 }}
    </label>
    {{/unless}}
    <section class="currency">
        {{#each currency}}
        <label>
            <i class="currency {{ @key }}" data-tooltip aria-label="{{ label }}"></i>
            {{ numberInput value name=(concat "amount." @key) class="uninput" min=0 step=1 }}
        </label>
        {{/each}}
    </section>
    <div class="destinations">
        {{#if noPrimaryParty}}
        <p class="no-primary-party">{{ localize "SHMN.Award.NoPrimaryParty" }}</p>
        {{/if}}
        {{#each destinations}}
        <label>
            <shmn-checkbox name="destination.{{ doc.id }}" {{ checked checked }}></shmn-checkbox>
            {{{ icon }}} {{ doc.name }}
        </label>
        {{else}}
        <p class="no-destinations">{{ localize "SHMN.Award.NoDestinations" }}</p>
        {{/each}}
    </div>
    <button type="submit" name="transfer" disabled>
        <i class="fa-solid fa-trophy" inert></i> {{ localize "SHMN.Award.Action" }}
    </button>
</section>
